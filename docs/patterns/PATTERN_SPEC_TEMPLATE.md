# Pattern Spec Template

Use this template to define a new pattern so the assistant and contributors can follow it consistently.

## 1. Intent and Scope
- What problem this pattern solves
- When to use it
- When not to use it

## 2. Folder Structure
- Target path(s): e.g., `src/modules/<feature>/<sub-feature>`
- Example tree:
